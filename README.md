# Sparks subgraph

Indexes NFTs on the Sparks platform.

## Deploying the subgraph:

**First time only**
```ssh
yarn install
```

```ssh
yarn prepare:<network>
yarn codegen
yarn build
yarn deploy:<network>
```
